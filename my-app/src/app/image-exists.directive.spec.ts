import { ImageExistsDirective } from './image-exists.directive';

describe('ImageExistsDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageExistsDirective();
    expect(directive).toBeTruthy();
  });
});
